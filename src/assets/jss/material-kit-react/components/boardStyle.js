import { primaryColor } from "assets/jss/material-kit-react";

const boardStyle = {
  board: {
    background: primaryColor,
    backgroundImage: "assets/img/dots.png",
    backgroundSize: "contain",
  },
};

export default boardStyle;
